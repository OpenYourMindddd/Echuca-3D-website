/**
 * Product catalogue — empty while the online store is in "coming soon" mode.
 *
 * When ready to launch, add items here and restore the grid on Products.jsx:
 *
 * export const products = [
 *   {
 *     id: 'mullet-guard',
 *     name: 'Mullet Guard',
 *     description: '...',
 *     category: 'Outdoor & Vehicle',
 *   },
 * ];
 */
export const products = [];

/** Set false when products are live and the grid is enabled again. */
export const catalogueComingSoon = true;
